<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.txl.testtvlib">

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".TestTvLibApplication"
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cardframe.draworder.CardFrameDrawOrderActivity"></activity>
        <activity android:name=".testborder.TestBorderViewActivity" />
        <activity android:name=".testborder.TestBorderActivity" />
        <activity android:name=".NavTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".testrecyclerview.item.center.TestRecyclerViewFocusCenterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".testborder.TestFlyBorderActivity" />
    </application>

</manifest>